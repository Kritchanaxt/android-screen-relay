# Android Screen Relay

Android Screen Relay คือแอปพลิเคชันระบบ Android ประสิทธิภาพสูงสำหรับการแชร์หน้าจอและควบคุมระยะไกลแบบ Peer-to-Peer ผ่านเครือข่าย Wi-Fi ภายใน (Local Network) แอปพลิเคชันนี้ทำงานโดยไม่ต้องใช้เซิร์ฟเวอร์ตัวกลางหรืออินเทอร์เน็ต โดยใช้ WebSocket Server ที่ฝังอยู่ในตัวเพื่อการสื่อสารที่มีความหน่วงต่ำที่สุด

## ✨ ฟีเจอร์หลัก (Key Features)

### 🖥️ การแชร์หน้าจอและการควบคุม (Screen Mirroring & Control)
*   **สตรีมผ่านเครือข่ายภายใน:** ส่งภาพหน้าจอไปยังอีกอุปกรณ์ที่อยู่ในวง WLAN เดียวกันได้โดยตรง
*   **การควบคุมระยะไกล (Touch/Input):** ควบคุมอุปกรณ์เครื่อง Host (ต้นทาง) จากเครื่อง Client (ปลายทาง) โดยใช้ Accessibility Service (รองรับการคลิกและการปัด)
*   **การสื่อสารสองทิศทาง:** ใช้สถาปัตยกรรม WebSocket จัดการทั้งการส่งภาพวิดีโอออกและการรับคำสั่งควบคุมเข้ามาพร้อมกัน

### 🛡️ การทำงานต่อเนื่องระดับระบบ (System-Level Persistence)
*   **ทำงานเบื้องหลังตลอดเวลา:** Service ทำงานต่อเนื่องแม้จะปิดหน้าจอหรือย่อแอป
*   **เริ่มทำงานอัตโนมัติเมื่อเปิดเครื่อง:** เปิด Service อัตโนมัติเมื่อบูตระบบโดยใช้ `BootReceiver`
*   **ความเสถียรสูง:** ตั้งค่าแบบ `START_STICKY` และยกเว้นการประหยัดพลังงาน (Battery Optimization) เพื่อป้องกันไม่ให้ระบบปิดการทำงานของแอป

### 📲 ประสบการณ์ผู้ใช้ (UI/UX)
*   **หน้าจอนำสมัย:** พัฒนาด้วย **Jetpack Compose** และ **Material 3** ทั้งหมด
*   **ตัวบ่งชี้อัจฉริยะ:** มี Overlay "On Air" ลอยอยู่เพื่อแสดงสถานะว่ากำลังถ่ายทอดสดหน้าจออยู่
*   **การเชื่อมต่อง่ายดาย:**
    *   **Host:** สร้าง QR Code แบบไดนามิกเพื่อใช้เชื่อมต่อ
    *   **Client:** มีระบบสแกน QR Code (CameraX) ในตัวเพื่อจับคู่ได้ทันที

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

*   **ภาษา:** Kotlin
*   **สถาปัตยกรรม:** MVVM / Component-based
*   **UI:** Jetpack Compose (Material 3)
*   **เครือข่าย:** [Java-WebSocket](https://github.com/TooTallNate/Java-WebSocket) (Embedded Server & Client)
*   **Android APIs หลัก:**
    *   `MediaProjectionManager`: สำหรับจับภาพหน้าจออย่างมีประสิทธิภาพ
    *   `AccessibilityService`: สำหรับจำลองการสัมผัสหน้าจอ
    *   `ForegroundService`: สำหรับรักษาระดับความสำคัญของ Process
    *   `BootReceiver`: สำหรับเหตุการณ์เริ่มระบบ
    *   `CameraX` & `ZXing`: สำหรับการสแกนและสร้าง QR Code

## 📱 วิธีใช้งาน (How to Use)

### 1. ข้อกำหนดเบื้องต้น
*   ตรวจสอบให้แน่ใจว่าทั้งสองอุปกรณ์เชื่อมต่อ **Wi-Fi วงเดียวกัน**
*   Android SDK ขั้นต่ำ: 24 (Android 7.0)

### 2. การตั้งค่าครั้งแรก (One-time Setup)
เพื่อเปิดใช้งานฟีเจอร์ระดับสูง ให้ไปที่แท็บ **Me > System Permissions**:
*   **Remote Control (Touch):** เปิด Accessibility Service เพื่ออนุญาตการคลิกจากระยะไกล
*   **Run in Background:** ปิด Battery Optimization เพื่อให้แอปทำงานได้ต่อเนื่องไม่มีสะดุด

### 3. เครื่องต้นทาง (Host / Sender)
1.  เลือกคุณภาพวิดีโอ (Video Quality) ที่ต้องการ (Fast, HD, FHD, 2K)
2.  แตะปุ่ม **"Start Broadcasting"** ที่หน้าจอหลัก
3.  กดอนุญาตการบันทึกหน้าจอ
4.  สัญลักษณ์ "On Air" สีเขียวจะปรากฏขึ้น
5.  แชร์ **QR Code** หรือรหัส Passkey ให้เครื่องปลายทาง

### 4. เครื่องปลายทาง (Client / Receiver)
1.  แตะ **ไอคอนสแกน** (มุมขวาบน) และสแกน QR Code ของ Host หรือใส่ IP และ Passkey
2.  ภาพหน้าจอจะปรากฏขึ้น
3.  **การโต้ตอบ:** แตะที่หน้าจอของ Client เพื่อส่งคำสั่งคลิกกลับไปยัง Host

## ⚙️ การติดตั้ง (Installation)

1.  Clone repository นี้
    ```bash
    git clone https://github.com/yourusername/android-screen-relay.git
    ```
2.  เปิดใน **Android Studio**
3.  Sync Gradle และกด Run

---
*พัฒนาเพื่อเป็นเครื่องมือระบบประสิทธิภาพสูงโดยใช้มาตรฐาน Android ยุคใหม่*

## 🔧 บันทึกการแก้ไขปัญหาและการปรับปรุงประสิทธิภาพ (Optimization & Troubleshooting Log)

### 1. คุณภาพของภาพ vs ประสิทธิภาพ (Image Quality vs. Performance)
- **ปัญหา:** ผู้ใช้รายงานว่าภาพแตกและอ่านตัวหนังสือไม่ออก ("ภาพแตก") สาเหตุมาจากการย่อขนาดภาพลงมากเกินไป (เหลือ 1/3) ในเวอร์ชันแรกเพื่อลดความหน่วง
- **การแก้ไข:** เพิ่มฟีเจอร์ **Selectable Video Quality** ให้ผู้ใช้เลือกความคมชัดได้ เขียนระบบจับภาพใหม่ให้รองรับการเปลี่ยนความละเอียดแบบไดนามิก:
    - **Fast:** ~480p (บีบอัดสูง ความหน่วงต่ำมาก)
    - **HD:** ~720p (สมดุล)
    - **FHD:** ~1080p (ตัวหนังสือชัดเจน)
    - **2K:** Native Resolution (ความละเอียดสูงสุด)
- **ผลลัพธ์:**
    - *ก่อนหน้า:* ภาพมัว ตัวหนังสือเล็กอ่านไม่ได้
    - *หลังแก้ไข:* เลือกคุณภาพได้ตามต้องการ รองรับทั้งการอ่านข้อความที่คมชัด หรือโหมดเกมมิ่งที่เน้นความเร็ว

### 2. แอปค้างเมื่อกดหยุด (App Freezing on "Stop")
- **ปัญหา:** เมื่อกด "Stop Sharing" แอปจะค้างหรือไม่ตอบสนอง ("แอปค้าง")
- **สาเหตุ:** `ScreenCaptureManager` ใช้คำสั่งรอ Thread (`backgroundThread.join()`) บน Main Thread ระหว่างคืนค่าทรัพยากร ทำให้เกิด ANR (Application Not Responding)
- **การแก้ไข:** ปรับโค้ด `stopCapture` ใหม่ให้ปิดการทำงานแบบ Non-blocking (`quitSafely()`) และครอบการคืนค่าทรัพยากร (VirtualDisplay, MediaProjection) ด้วย `try-catch` เพื่อป้องกันข้อผิดพลาด
- **ผลลัพธ์:**
    - *ก่อนหน้า:* ค้าง 3-5 วินาที หรือต้อง Force Close
    - *หลังแก้ไข:* หยุดการทำงานได้ทันที ลื่นไหล และปลอดภัย

### 3. การแสดงผล UI และ Accessibility (UI Layout)
- **ปัญหา:** ปุ่ม "Stop Sharing" หายไปในหน้าจอขนาดเล็กหรือเมื่อเปิดฟอนต์ขนาดใหญ่ ("ปุ่มหาย")
- **สาเหตุ:** หน้าจอหลัก (Home Screen) ไม่รองรับการเลื่อน (Scroll) และมีการกำหนดความสูงของปุ่มแบบตายตัวทำให้เนื้อหาล้นจอ
- **การแก้ไข:** 
    - เพิ่ม `verticalScroll` ให้กับ Container หลักของหน้าจอ
    - ลบการกำหนดความสูงตายตัว (`height`) ออกจากปุ่ม และใช้ `defaultMinSize` แทนเพื่อให้ปุ่มขยายขนาดตามเนื้อหาได้
- **ผลลัพธ์:**
    - *ก่อนหน้า:* ปุ่มคำสั่งสำคัญถูกตัดหายไป ใช้งานไม่ได้ในบางอุปกรณ์
    - *หลังแก้ไข:* รองรับทุกขนาดหน้าจอและการตั้งค่าฟอนต์ สามารถเลื่อนลงมาเพื่อกดปุ่มได้

### 4. ประสิทธิภาพโปรโตคอล (Protocol Efficiency)
- **ปัญหา:** ความหน่วงสูง (Latency) และความเสี่ยงด้านความปลอดภัย
- **การแก้ไข:** 
    - เปลี่ยนจากการเข้ารหัส Base64 String เป็น **Raw Binary Streams** (ลดขนาดข้อมูลได้ ~30%)
    - ใช้ระบบ **Zero-Delay Authentication** บังคับตรวจสอบ Passkey ทันทีที่เชื่อมต่อ WebSocket
- **ผลลัพธ์:** ลดความหน่วงจนเกือบ Real-time (<200ms) และป้องกันการเชื่อมต่อที่ไม่ได้รับอนุญาต
